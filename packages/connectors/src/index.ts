/**
 * `@chief-of-staff/connectors` — the channel connector interface (`ingest`, `send?`, `identity`)
 * and a reusable contract-test suite factory. Types only; per-channel implementations start in
 * Task 3 (Gmail).
 *
 * Testing utilities are available via the `./testing` subpath (requires vitest).
 */

export * from './types.js';
