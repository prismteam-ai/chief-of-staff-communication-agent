/**
 * Placeholder entry point for the channel connector package.
 *
 * Task 2 fills this in with the connector interface (`ingest`, `send?`,
 * `identity`); Tasks 3/9/12 add per-channel implementations. Kept
 * empty-but-compiling here so the workspace graph and CI pipelines are
 * exercised from day one.
 */
export const CONNECTORS_PACKAGE_PLACEHOLDER = true;
