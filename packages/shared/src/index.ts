/**
 * Placeholder entry point for the shared domain package.
 *
 * Task 2 fills this in with `NormalizedMessage`, the communication state
 * machine, the account model, and the per-user permission guard. Kept
 * empty-but-compiling here so downstream packages can already depend on
 * `@chief-of-staff/shared` via the pnpm workspace protocol.
 */
export const SHARED_PACKAGE_PLACEHOLDER = true;
